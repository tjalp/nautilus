name: Nautilus
version: '1.0'
main: net.tjalp.nautilus.Nautilus
api-version: '1.20'
authors: [tjalp]
dependencies:
  server:
    LibsDisguises:
      load: BEFORE
      required: false
    TAB:
      load: BEFORE
      required: false
    ProtocolLib:
      load: BEFORE
      required: false
    floodgate:
      load: BEFORE
      required: false